#include<bits/stdc++.h>
using namespace std;
int main()
{
    int a,N,i;

    cin>>a>>N;
    pair<int ,int> team[N];
    for(i=0;i<N;i++)
    {
        cin>>team[i].first>>team[i].second;
    }
    sort
}
